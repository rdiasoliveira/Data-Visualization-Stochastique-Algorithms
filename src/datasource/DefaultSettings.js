export default class DefaultSettings {
    
    static data = {
        /* Dont forget to add the final slash '/' in the defaultDataPath */
        defaultDataPath: "ADS-DadosDeExecucaoDeAlgoritmos/",
        defaultDelimiter: ";",
        defaultFilePattern: "^.*(_run[0-9]*\\.csv)$",
        defaultIgnoreFirstLine: true
    };

}